Release v0.2.2: fix npx bin resolution, interactive installer, CI + drift

- package.json: add 'realtime-register-skills' bin alias so
  'npx @cave-man/realtime-register-skills <subcommand>' resolves to the
  installer entry (fixes 'could not determine executable to run').
- src/cli: read version from package.json via createRequire.
- src/cli/commands/install.ts: TTY-aware interactive target picker
  via @inquirer/prompts; non-TTY / --yes / piped input unchanged.
- tests/: new vitest suite (22 tests) for skill-paths, spec loader,
  install/uninstall round-trip.
- .github/workflows/: CI (Node 20 and 22, build/lint/audit/test plus doctor
  on main), Release (tag-triggered npm publish --provenance, guarded
  on NPM_TOKEN), Drift (weekly scrape plus diff, opens issue on drift).
- scripts/diff-live.mjs: scrape and diff script backing the drift job.
- README / CHANGELOG / HANDOVER: v0.2.2 savepoint, corrected sample
  operationIds (createDomain), interactive installer note.
